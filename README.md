# eztvapi.re wrapper

## Default API Methods
* getPages()
* getShows(page, options = {})
* getDetails(id)

## Custom API Methods
These methods are in place to have some more comfort working with the API.